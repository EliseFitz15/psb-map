

<script type="text/javascript">

var request = $.ajax({
  url: "https://maps.googleapis.com/maps/api/geocode/json?&address=02210",
  type: 'GET',
  success: function(response) {
    alert("Success");
    console.log(response)
    var latlng = response["results"][0]["geometry"]["location"];
    console.log(latlng);
  }
});
var north = data["results"][0]["geometry"]["bounds"]["northeast"]["lat"]
var east = data["results"][0]["geometry"]["bounds"]["northeast"]["lng"]
var south = data["results"][0]["geometry"]["bounds"]["southwest"]["lat"]
var west = data["results"][0]["geometry"]["bounds"]["southwest"]["lng"]

var rectangle = new google.maps.Rectangle({
  strokeColor: '#FF0000',
  strokeOpacity: 0.8,
  strokeWeight: 2,
  fillColor: '#FFFFFF',
  fillOpacity: 0.05,
  map: map,
  bounds: {
    north: north,
    south: south,
    east: east,
    west: west
  }
});
});
</script>
